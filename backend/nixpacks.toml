[phases.setup]
nixPkgs = ["chromium", "nss", "freetype", "harfbuzz", "ca-certificates", "fonts-liberation"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "node dist/index.js"
